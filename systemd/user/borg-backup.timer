[Unit]
Description=Daily borg backup of $HOME

[Timer]
OnCalendar=daily
# The laptop is often asleep at the scheduled time; run at next opportunity
# instead of skipping the day.
Persistent=true
RandomizedDelaySec=30m

[Install]
WantedBy=timers.target
